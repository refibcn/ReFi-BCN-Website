diff --git a/.cursorrules/README.md b/.cursorrules/README.md
index 473d1fe..c496370 100644
--- a/.cursorrules/README.md
+++ b/.cursorrules/README.md
@@ -202,3 +202,13 @@ For questions or updates, contact the ReFi Barcelona team at hola@refibcn.cat.
 
 
 
+
+
+
+
+
+
+
+
+
+
diff --git a/DEPLOY_INSTRUCTIONS.md b/DEPLOY_INSTRUCTIONS.md
index 8d88a57..a288b88 100644
--- a/DEPLOY_INSTRUCTIONS.md
+++ b/DEPLOY_INSTRUCTIONS.md
@@ -12,7 +12,7 @@ The site is already configured with:
 Run these commands from the ReFi-BCN-Website directory:
 
 ```bash
-cd "/Users/luizfernando/Desktop/git/Zettelkasten/03 Libraries/ReFi-BCN-Website"
+cd "/Users/luizfernando/Desktop/Workspaces/Zettelkasten/03 Libraries/ReFi-BCN-Website"
 
 # Stage all changes
 git add .
diff --git a/HTTPS-TROUBLESHOOTING.md b/HTTPS-TROUBLESHOOTING.md
index c33b536..294b84c 100644
--- a/HTTPS-TROUBLESHOOTING.md
+++ b/HTTPS-TROUBLESHOOTING.md
@@ -123,7 +123,7 @@ Or use online tools:
 
 **Check:**
 ```bash
-cd "/Users/luizfernando/Desktop/git/Zettelkasten/03 Libraries/ReFi-BCN-Website"
+cd "/Users/luizfernando/Desktop/Workspaces/Zettelkasten/03 Libraries/ReFi-BCN-Website"
 cat quartz/static/CNAME
 ```
 
@@ -216,7 +216,7 @@ dig refibcn.cat +short
 curl -I https://refibcn.cat
 
 # Check GitHub Pages deployment
-cd "/Users/luizfernando/Desktop/git/Zettelkasten/03 Libraries/ReFi-BCN-Website"
+cd "/Users/luizfernando/Desktop/Workspaces/Zettelkasten/03 Libraries/ReFi-BCN-Website"
 git log -1 --oneline
 ```
 
@@ -329,3 +329,13 @@ Always use these four IPs for A records:
 
 
 
+
+
+
+
+
+
+
+
+
+
diff --git a/content/index.md b/content/index.md
index e035bc8..b21a26e 100644
--- a/content/index.md
+++ b/content/index.md
@@ -4,21 +4,23 @@ description: "Connecting Catalonia's regenerative movements with global innovati
 ---
 
 <div class="hero-section">
-  <div class="hero-top">
-    <div class="hero-title">
-      <h1>Seeding pathways toward just and resilient economies</h1>
+  <div class="hero-inner">
+    <div class="hero-top">
+      <div class="hero-title">
+        <h1>Seeding pathways toward just and resilient economies</h1>
+      </div>
+      <div class="hero-image">
+        <img src="/static/hero_home.png" alt="ReFi BCN hero illustration"/>
+      </div>
     </div>
-    <div class="hero-image">
-      <img src="/static/hero_home.png" alt="ReFi BCN hero illustration"/>
+    <div class="hero-bottom">
+      <p class="hero-text">
+        We connect Catalonia's regenerative movements with global innovations in finance, 
+        technology, and governance to accelerate the transition toward a life-centered economy.
+      </p>
+      <a href="#about" class="cta-button">Learn More</a>
     </div>
   </div>
-  <div class="hero-bottom">
-    <p class="hero-text">
-      We connect Catalonia's regenerative movements with global innovations in finance, 
-      technology, and governance to accelerate the transition toward a life-centered economy.
-    </p>
-    <a href="#about" class="cta-button">Learn More</a>
-  </div>
 </div>
 
 <div id="about" class="section-container">
diff --git a/quartz/components/Navigation.tsx b/quartz/components/Navigation.tsx
index ef8d712..477bd14 100644
--- a/quartz/components/Navigation.tsx
+++ b/quartz/components/Navigation.tsx
@@ -4,80 +4,152 @@ import style from "./styles/navigation.scss"
 export default (() => {
   const Navigation: QuartzComponent = ({ displayClass }: QuartzComponentProps) => {
     return (
-      <nav class={`navigation ${displayClass ?? ""}`}>
-        <button 
-          id="mobile-menu-button"
-          class="mobile-nav-trigger" 
-          aria-label="Toggle menu"
-        >
-          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
-            <line x1="3" y1="12" x2="21" y2="12"></line>
-            <line x1="3" y1="6" x2="21" y2="6"></line>
-            <line x1="3" y1="18" x2="21" y2="18"></line>
-          </svg>
-        </button>
-        <ul id="nav-menu" class="nav-links">
-          <li><a href="#about">About</a></li>
-          <li><a href="#events">Events</a></li>
-          <li><a href="#regenerant-catalunya">Regenerant Catalunya</a></li>
-          <li><a href="#ecosystem-map">Ecosystem Map</a></li>
-          <li><a href="#contact">Contact</a></li>
-        </ul>
-        {/* Overlay for closing menu */}
-        <div id="nav-menu-overlay" class="nav-menu-overlay"></div>
+      <nav class={`navigation ${displayClass ?? ""}`} aria-label="Primary navigation">
+        <div class="nav-shell">
+          <ul id="nav-menu" class="nav-links">
+            <li><a href="#about">About</a></li>
+            <li><a href="#events">Events</a></li>
+            <li><a href="#regenerant-catalunya">Regenerant Catalunya</a></li>
+            <li><a href="#ecosystem-map">Ecosystem Map</a></li>
+            <li><a href="#contact">Contact</a></li>
+          </ul>
+          <button
+            class="nav-toggle action-button"
+            type="button"
+            aria-controls="nav-menu"
+            aria-expanded="false"
+            aria-label="Toggle navigation menu"
+            data-nav-toggle
+          >
+            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+              <line x1="3" y1="12" x2="21" y2="12"></line>
+              <line x1="3" y1="6" x2="21" y2="6"></line>
+              <line x1="3" y1="18" x2="21" y2="18"></line>
+            </svg>
+          </button>
+        </div>
       </nav>
     )
   }
 
   Navigation.css = style
   Navigation.afterDOMLoaded = `
-    document.addEventListener('DOMContentLoaded', function() {
-      const menuButton = document.getElementById('mobile-menu-button');
-      const navMenu = document.getElementById('nav-menu');
-      const overlay = document.getElementById('nav-menu-overlay');
-      
-      if (menuButton && navMenu && overlay) {
-        // Toggle menu when clicking the button
-        menuButton.addEventListener('click', function(e) {
-          e.preventDefault();
-          e.stopPropagation();
-          
-          const isOpen = navMenu.classList.contains('open');
-          
-          if (isOpen) {
-            navMenu.classList.remove('open');
-            overlay.classList.remove('open');
-          } else {
-            navMenu.classList.add('open');
-            overlay.classList.add('open');
-          }
-        });
-        
-        // Close menu when clicking overlay
-        overlay.addEventListener('click', function() {
-          navMenu.classList.remove('open');
-          overlay.classList.remove('open');
-        });
-        
-        // Close menu when pressing escape
-        document.addEventListener('keydown', function(e) {
-          if (e.key === 'Escape' && navMenu.classList.contains('open')) {
-            navMenu.classList.remove('open');
-            overlay.classList.remove('open');
-          }
-        });
-        
-        // Close menu when clicking menu items
-        const menuLinks = navMenu.querySelectorAll('a');
-        menuLinks.forEach(link => {
-          link.addEventListener('click', function() {
-            navMenu.classList.remove('open');
-            overlay.classList.remove('open');
-          });
-        });
+    document.addEventListener('DOMContentLoaded', () => {
+      const navigation = document.querySelector('.navigation');
+      const navShell = navigation?.querySelector('.nav-shell');
+      const navMenu = navigation?.querySelector('#nav-menu');
+      const menuButton = navigation?.querySelector('[data-nav-toggle]');
+
+      if (!navigation || !navShell || !navMenu || !menuButton) {
+        return;
       }
+
+      let isCollapsed = navigation.hasAttribute('data-collapsed');
+      let rafId = null;
+
+      const closeMenu = () => {
+        navMenu.classList.remove('open');
+        navShell.classList.remove('menu-open');
+        menuButton.setAttribute('aria-expanded', 'false');
+      };
+
+      const openMenu = () => {
+        navMenu.classList.add('open');
+        navShell.classList.add('menu-open');
+        menuButton.setAttribute('aria-expanded', 'true');
+        const firstLink = navMenu.querySelector('a');
+        if (firstLink instanceof HTMLElement) {
+          firstLink.focus();
+        }
+      };
+
+      const measureNavWidth = () => {
+        const originalStyle = navMenu.getAttribute('style') ?? '';
+        navMenu.setAttribute(
+          'style',
+          [
+            originalStyle,
+            'position:absolute !important',
+            'visibility:hidden !important',
+            'display:flex !important',
+            'flex-direction:row !important',
+            'flex-wrap:nowrap !important',
+            'white-space:nowrap !important',
+            'height:auto !important',
+            'max-width:none !important',
+          ].join(';')
+        );
+        const width = navMenu.scrollWidth;
+        if (originalStyle) {
+          navMenu.setAttribute('style', originalStyle);
+        } else {
+          navMenu.removeAttribute('style');
+        }
+        return width;
+      };
+
+      const updateCollapsedState = () => {
+        rafId = null;
+        const available = navShell.clientWidth;
+        const needed = measureNavWidth();
+        const forceMobile = window.innerWidth <= 768;
+        const shouldCollapse = forceMobile || needed + 48 > available;
+
+        if (shouldCollapse === isCollapsed) {
+          return;
+        }
+
+        isCollapsed = shouldCollapse;
+
+        if (shouldCollapse) {
+          navigation.setAttribute('data-collapsed', 'true');
+        } else {
+          navigation.removeAttribute('data-collapsed');
+          closeMenu();
+        }
+      };
+
+      const scheduleUpdate = () => {
+        if (rafId !== null) {
+          return;
+        }
+        rafId = window.requestAnimationFrame(updateCollapsedState);
+      };
+
+      const resizeObserver = new ResizeObserver(scheduleUpdate);
+      resizeObserver.observe(navigation);
+      window.addEventListener('resize', scheduleUpdate);
+      updateCollapsedState();
+
+      menuButton.addEventListener('click', (event) => {
+        event.preventDefault();
+        event.stopPropagation();
+        const isOpen = navMenu.classList.contains('open');
+        if (isOpen) {
+          closeMenu();
+        } else {
+          openMenu();
+        }
+      });
+
+      document.addEventListener('keydown', (event) => {
+        if (event.key === 'Escape' && navMenu.classList.contains('open')) {
+          closeMenu();
+          menuButton.focus();
+        }
+      });
+
+      document.addEventListener('click', (event) => {
+        if (!navigation.contains(event.target)) {
+          closeMenu();
+        }
+      });
+
+      navMenu.querySelectorAll('a').forEach((link) => {
+        link.addEventListener('click', closeMenu);
+      });
     });
   `
-  
+
   return Navigation
 }) satisfies QuartzComponentConstructor
diff --git a/quartz/components/Search.tsx b/quartz/components/Search.tsx
index 6e932d2..200e77f 100644
--- a/quartz/components/Search.tsx
+++ b/quartz/components/Search.tsx
@@ -16,30 +16,36 @@ const defaultOptions: SearchOptions = {
 export default ((userOpts?: Partial<SearchOptions>) => {
   const Search: QuartzComponent = ({ displayClass, cfg }: QuartzComponentProps) => {
     const opts = { ...defaultOptions, ...userOpts }
-    const searchPlaceholder = i18n(cfg.locale).components.search.searchBarPlaceholder
+    const searchCopy = i18n(cfg.locale).components.search
+    const searchPlaceholder = searchCopy.searchBarPlaceholder
+    const searchTitle = searchCopy.title
     return (
-      <div class={classNames(displayClass, "search")}>
-        <button class="search-button">
-          <svg role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 19.9 19.7">
-            <title>Search</title>
-            <g class="search-path" fill="none">
-              <path stroke-linecap="square" d="M18.5 18.3l-5.4-5.4" />
-              <circle cx="8" cy="8" r="7" />
-            </g>
-          </svg>
-          <p>{i18n(cfg.locale).components.search.title}</p>
-        </button>
-        <div class="search-container">
-          <div class="search-space">
-            <input
-              autocomplete="off"
-              class="search-bar"
-              name="search"
-              type="text"
-              aria-label={searchPlaceholder}
-              placeholder={searchPlaceholder}
-            />
-            <div class="search-layout" data-preview={opts.enablePreview}></div>
+      <div class={classNames(displayClass, "header-actions")}>
+        <div class="search">
+          <button class="search-button search-button--compact action-button" type="button" aria-label={searchTitle}>
+            <span class="search-button__icon" aria-hidden="true">
+              <svg role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 19.9 19.7">
+                <title>{searchTitle}</title>
+                <g class="search-path" fill="none">
+                  <path stroke-linecap="square" d="M18.5 18.3l-5.4-5.4" />
+                  <circle cx="8" cy="8" r="7" />
+                </g>
+              </svg>
+            </span>
+            <p class="search-button__label">{searchTitle}</p>
+          </button>
+          <div class="search-container">
+            <div class="search-space">
+              <input
+                autocomplete="off"
+                class="search-bar"
+                name="search"
+                type="text"
+                aria-label={searchPlaceholder}
+                placeholder={searchPlaceholder}
+              />
+              <div class="search-layout" data-preview={opts.enablePreview}></div>
+            </div>
           </div>
         </div>
       </div>
diff --git a/quartz/components/scripts/navigation.inline.ts b/quartz/components/scripts/navigation.inline.ts
deleted file mode 100644
index 673cdb2..0000000
--- a/quartz/components/scripts/navigation.inline.ts
+++ /dev/null
@@ -1,38 +0,0 @@
-export default () => {
-  const menuButton = document.getElementById("mobile-menu-button")
-  const navMenu = document.getElementById("nav-menu")
-  const overlay = document.getElementById("nav-menu-overlay")
-  
-  if (!menuButton || !navMenu || !overlay) {
-    return
-  }
-  
-  menuButton.addEventListener("click", (e) => {
-    e.preventDefault()
-    e.stopPropagation()
-    
-    const isOpen = navMenu.classList.contains("open")
-    navMenu.classList.toggle("open", !isOpen)
-    overlay.classList.toggle("open", !isOpen)
-  })
-  
-  overlay.addEventListener("click", () => {
-    navMenu.classList.remove("open")
-    overlay.classList.remove("open")
-  })
-  
-  document.addEventListener("keydown", (e) => {
-    if (e.key === "Escape" && navMenu.classList.contains("open")) {
-      navMenu.classList.remove("open")
-      overlay.classList.remove("open")
-    }
-  })
-  
-  const menuLinks = navMenu.querySelectorAll("a")
-  menuLinks.forEach(link => {
-    link.addEventListener("click", () => {
-      navMenu.classList.remove("open")
-      overlay.classList.remove("open")
-    })
-  })
-}
diff --git a/quartz/components/styles/search.scss b/quartz/components/styles/search.scss
index bb58948..b9627d4 100644
--- a/quartz/components/styles/search.scss
+++ b/quartz/components/styles/search.scss
@@ -8,33 +8,32 @@
   }
 
   & > .search-button {
-    background-color: transparent;
-    border: 1px var(--lightgray) solid;
-    border-radius: 4px;
-    font-family: inherit;
-    font-size: inherit;
-    height: 2rem;
-    padding: 0 1rem 0 0;
-    display: flex;
+    width: var(--action-btn-size);
+    height: var(--action-btn-size);
+    min-width: var(--action-btn-size);
+    min-height: var(--action-btn-size);
+    display: inline-flex;
     align-items: center;
-    text-align: inherit;
+    justify-content: center;
+    border: var(--action-btn-border);
+    background: var(--action-btn-bg);
+    box-shadow: var(--action-btn-shadow);
+    padding: 0;
+    gap: 0;
     cursor: pointer;
-    white-space: nowrap;
-    width: 100%;
 
     & > p {
-      display: inline;
-      color: var(--gray);
+      display: none;
     }
 
     & svg {
       cursor: pointer;
-      width: 18px;
-      min-width: 18px;
-      margin: 0 0.5rem;
+      width: 20px;
+      min-width: 20px;
+      margin: 0;
 
       .search-path {
-        stroke: var(--darkgray);
+        stroke: var(--blue-dark);
         stroke-width: 1.5px;
         transition: stroke 0.5s ease;
       }
diff --git a/quartz/styles/custom.scss b/quartz/styles/custom.scss
index e938482..deb1126 100644
--- a/quartz/styles/custom.scss
+++ b/quartz/styles/custom.scss
@@ -41,6 +41,37 @@ body {
   --action-btn-bg: color-mix(in srgb, var(--light) 95%, rgba(255, 255, 255, 0.6));
   --action-btn-bg-hover: color-mix(in srgb, var(--light) 88%, rgba(255, 255, 255, 0.8));
   --action-btn-shadow: 0 15px 35px rgba(9, 32, 69, 0.17);
+  --layout-inline: clamp(1.5rem, 5vw, 3.5rem);
+}
+
+@media (max-width: 1024px) {
+  :root {
+    --layout-inline: clamp(1.25rem, 5vw, 3rem);
+  }
+}
+
+@media (max-width: 768px) {
+  :root {
+    --layout-inline: clamp(1.1rem, 4.5vw, 2.25rem);
+  }
+}
+
+@media (max-width: 640px) {
+  :root {
+    --layout-inline: 1rem;
+  }
+}
+
+@media (max-width: 480px) {
+  :root {
+    --layout-inline: 0.85rem;
+  }
+}
+
+@media (max-width: 375px) {
+  :root {
+    --layout-inline: 0.75rem;
+  }
 }
 
 // ============================================
@@ -133,31 +164,15 @@ body {
 .page {
   max-width: 800px !important;
   margin: 0 auto !important;
-  padding: 0 2rem !important;
+  padding: 0 var(--layout-inline) !important;
   transition: padding 0.2s ease;
-  
-  @media (max-width: 639px) {
-    padding: 0 1rem !important;
-  }
-  
-  @media (max-width: 375px) {
-    padding: 0 0.75rem !important;
-  }
 }
 
 #quartz-body {
   max-width: 800px !important;
   margin: 0 auto !important;
-  padding: 0 2rem !important;
+  padding: 0 var(--layout-inline) !important;
   transition: padding 0.2s ease;
-  
-  @media (max-width: 639px) {
-    padding: 0 1rem !important;
-  }
-  
-  @media (max-width: 375px) {
-    padding: 0 0.75rem !important;
-  }
 }
 
 // ============================================
@@ -173,11 +188,11 @@ header {
   z-index: 100;
   background-color: var(--light);
   width: 100%;
-  max-width: calc(100% - 3rem); // 1.5rem on each side
+  max-width: calc(100% - (var(--layout-inline) * 2));
   margin: 0.5rem auto 0;
   border: 1.5px solid var(--blue-dark);
   border-top: 1.5px solid var(--blue-dark);
-  padding: 0.5rem clamp(1rem, 4vw, 2.5rem);
+  padding: 0.5rem var(--layout-inline);
   display: grid;
   grid-template-columns: auto minmax(0, 1fr) auto;
   align-items: center;
@@ -191,15 +206,11 @@ header {
   }
   
   @media (max-width: 640px) {
-    max-width: calc(100% - 1rem);
-    padding: 0.5rem 1rem;
     column-gap: 1rem;
     margin-top: 0.25rem;
   }
   
   @media (max-width: 375px) {
-    max-width: calc(100% - 0.5rem);
-    padding: 0.5rem 0.75rem;
     column-gap: 0.5rem;
   }
 }
@@ -260,8 +271,8 @@ header .search .search-button--compact {
   min-height: var(--action-btn-size);
   padding: 0;
   gap: 0;
-  border: var(--action-btn-border) !important;
-  background: color-mix(in srgb, var(--light) 95%, rgba(255, 255, 255, 0.6)) !important;
+  border: var(--action-btn-border);
+  background: var(--action-btn-bg);
   box-shadow: var(--action-btn-shadow);
   justify-content: center;
 }
@@ -1276,7 +1287,7 @@ article[data-slug="index"] {
 
   .hero-inner {
     margin: 0 auto;
-    padding-inline: clamp(1.5rem, 5vw, 4rem);
+    padding-inline: var(--layout-inline);
     width: min(1200px, 100%);
     display: flex;
     flex-direction: column;
@@ -1387,16 +1398,21 @@ article[data-slug="index"] {
   // Hero image styles (applies to all hero sections)
   .hero-image {
     position: relative;
-    width: min(48vw, 480px);
+    width: clamp(280px, 48vw, 520px);
     max-width: 100%;
     justify-self: center;
     
+    @media (max-width: 960px) {
+      width: min(100%, 480px);
+    }
+    
     img {
       display: block;
       width: 100%;
       height: auto;
       aspect-ratio: 5 / 4;
-      border: 1.5px solid var(--blue-dark);
+      border: none;
+      box-shadow: var(--action-btn-shadow);
       object-fit: cover;
     }
   }
@@ -1410,70 +1426,39 @@ article[data-slug="index"] {
 }
 
 // Section with background tint
-.section-tinted {
-  background: var(--blue-highlight);
-  padding: 3rem 1.5rem;
-  margin: 3rem -1.5rem;
+.section-tinted,
+.section-container,
+.callout-section {
+  width: min(1200px, calc(100% - 2 * var(--layout-inline)));
+  margin: clamp(2.5rem, 5vw, 3.5rem) auto;
+  padding-block: clamp(2.25rem, 4vw, 3.5rem);
+  padding-inline: clamp(1.5rem, 4vw, 3rem);
   border: 2px solid var(--blue-dark);
-  
-  @media (min-width: 640px) {
-    padding: 3rem 2rem;
-    margin: 3rem -2rem;
-  }
-  
-  @media (max-width: 639px) {
-    padding: 1.5rem 1rem;
-    margin: 2rem 0;
-  }
-  
-  @media (max-width: 375px) {
-    padding: 1.25rem 0.75rem;
-    margin: 1.5rem 0;
-  }
+  box-sizing: border-box;
+}
+
+.section-tinted,
+.section-container.section-container-tinted,
+.callout-section {
+  background: var(--blue-highlight);
 }
 
-// Section containers with borders (for alternating visual rhythm)
 .section-container {
-  border: 2px solid var(--blue-dark);
-  padding: 3rem 1.5rem;
-  margin: 3rem -1.5rem;
-  
-  @media (min-width: 640px) {
-    padding: 3rem 2rem;
-    margin: 3rem -2rem;
-  }
-  
-  @media (max-width: 639px) {
-    padding: 1.5rem 1rem;
-    margin: 2rem 0;
-  }
-  
-  @media (max-width: 375px) {
-    padding: 1.25rem 0.75rem;
-    margin: 1.5rem 0;
-  }
-  
-  // Variant with background for alternating sections
-  &.section-container-tinted {
-    background: var(--blue-highlight);
-  }
-  
-  // Remove extra top margin on section titles within containers
-  .section-title {
-    margin-top: 0;
-  }
-  
-  // Fix top margins for first child elements
-  > *:first-child,
-  > .section-content-centered > *:first-child,
-  > .hero-section > div > *:first-child {
-    margin-top: 0;
-  }
-  
-  // Last section container should have no bottom margin
-  &:last-of-type {
-    margin-bottom: 0;
-  }
+  background: transparent;
+}
+
+.section-container .section-title {
+  margin-top: 0;
+}
+
+.section-container > *:first-child,
+.section-container > .section-content-centered > *:first-child,
+.section-container > .hero-section > div > *:first-child {
+  margin-top: 0;
+}
+
+.section-container:last-of-type {
+  margin-bottom: 0;
 }
 
 // Standard section spacing
@@ -1871,8 +1856,8 @@ article[data-slug="index"] {
 // Team grid for "About Us" (3 columns)
 .team-grid {
   display: grid;
-  grid-template-columns: 1fr;
-  gap: 1.5rem;
+  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
+  gap: clamp(1rem, 2vw, 2rem);
   margin-top: 3rem;
   
   @media (max-width: 639px) {
@@ -1885,20 +1870,15 @@ article[data-slug="index"] {
     margin-top: 1.5rem;
   }
   
-  @media (min-width: 640px) {
-    grid-template-columns: repeat(2, 1fr);
-  }
-  
-  @media (min-width: 1024px) {
-    grid-template-columns: repeat(3, 1fr);
-  }
-  
   .team-member {
     border: 2px solid var(--blue-dark);
-    padding: 2rem 1rem;
+    padding: 2rem 1.25rem;
     background: transparent;
     transition: all 0.2s ease;
     text-align: center;
+    display: flex;
+    flex-direction: column;
+    height: 100%;
     
     @media (max-width: 639px) {
       padding: 1.5rem 0.75rem;
@@ -1916,30 +1896,9 @@ article[data-slug="index"] {
     img {
       width: 100%;
       height: auto;
-      margin-bottom: 1.5rem;
-      margin-left: -1rem;
-      margin-right: -1rem;
-      margin-top: -2rem;
-      width: calc(100% + 2rem);
-      max-width: calc(100% + 2rem);
+      margin: -1.5rem 0 1.25rem;
+      display: block;
       border-bottom: 2px solid var(--blue-dark);
-      
-      @media (max-width: 639px) {
-        margin-left: -0.75rem;
-        margin-right: -0.75rem;
-        margin-top: -1.5rem;
-        width: calc(100% + 1.5rem);
-        max-width: calc(100% + 1.5rem);
-        margin-bottom: 1rem;
-      }
-      
-      @media (max-width: 375px) {
-        margin-left: -0.625rem;
-        margin-right: -0.625rem;
-        margin-top: -1.25rem;
-        width: calc(100% + 1.25rem);
-        max-width: calc(100% + 1.25rem);
-      }
     }
     
     .team-name {
@@ -1995,6 +1954,7 @@ article[data-slug="index"] {
       line-height: 1.6;
       font-size: 0.9rem;
       margin-top: 0;
+      flex-grow: 1;
       
       @media (max-width: 375px) {
         font-size: 0.8125rem;
@@ -2005,27 +1965,8 @@ article[data-slug="index"] {
 
 // Callout section (for Regenerant Catalunya highlight)
 .callout-section {
-  background: var(--blue-highlight);
-  border: 2px solid var(--blue-dark);
-  padding: 3rem 1.5rem;
-  margin: 3rem -1.5rem;
   text-align: center;
   
-  @media (min-width: 640px) {
-    padding: 3rem 2rem;
-    margin: 3rem -2rem;
-  }
-  
-  @media (max-width: 639px) {
-    padding: 1.5rem 1rem;
-    margin: 2rem 0;
-  }
-  
-  @media (max-width: 375px) {
-    padding: 1.25rem 0.75rem;
-    margin: 1.5rem 0;
-  }
-  
   // Fix top margins for first child elements
   > *:first-child {
     margin-top: 0;
